#!/usr/bin/env node

'use strict'

require('../lib/cli/index').cli(process.argv.slice(2)).then(() => {
  process.exit(0)
}).catch(error => {
  console.error(`Oh no! Something went wrong: ${error.message}`)

  process.exit(1)
})
